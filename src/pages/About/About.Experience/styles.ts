import styled from "styled-components";

export const ExperienceCard = styled.div`
	display: flex;
	flex-direction: column;
	gap: 0.25rem;

	.name {
	}

	.local {
		font-weight: bold;
	}

	.time {
		opacity: 0.8;
	}
`;
