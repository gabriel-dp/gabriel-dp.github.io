import styled from "styled-components";

export const ObjectPDF = styled.object`
	height: 75svh;
	width: calc(100% - 2rem);
	border: none;

	display: flex;
	flex-direction: column;
	align-items: center;
`;
